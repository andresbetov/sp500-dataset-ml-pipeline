[project]
name = "sp500-dataset-ml-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "kagglehub",
    "pandas",
    "numpy",
    "pyarrow>=24.0.0",
    "matplotlib",
    "scipy",
]
